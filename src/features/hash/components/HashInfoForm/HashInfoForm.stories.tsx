import type { Meta, StoryObj } from '@storybook/react';

import { HashInfoForm } from './HashInfoForm';

type Story = StoryObj<typeof HashInfoForm>;

export default { component: HashInfoForm } as Meta<typeof HashInfoForm>;

export const Default: Story = {};
